# Litecoin Knowledge Hub - Environment Variables Template
# Copy this file to the appropriate environment file and fill in the values
#
# Environment Files:
# - .env.local              - Local development (localhost URLs)
# - .env.docker.dev         - Docker development (service names)
# - .env.docker.prod        - Docker production (service names, production URLs)
# - .env.stage              - Staging environment
#
# Service-Specific .env Files (for secrets only):
# - backend/.env            - Backend secrets (GOOGLE_API_KEY, etc.)
# - payload_cms/.env        - Payload CMS secrets (PAYLOAD_SECRET, etc.)

# =============================================================================
# SERVICE URLs (Different per environment)
# =============================================================================

# Backend API URL
# - Local: http://localhost:8000
# - Docker Dev: http://litecoin-backend:8000 (internal) or http://localhost:8000 (external)
# - Docker Prod: http://litecoin-backend:8000 (internal) or https://api.lite.space (external)
BACKEND_URL=http://localhost:8000

# Payload CMS Public Server URL (used by Payload admin panel)
# - Local: http://localhost:3001
# - Docker Dev: http://localhost:3001
# - Docker Prod: https://cms.lite.space
PAYLOAD_PUBLIC_SERVER_URL=http://localhost:3001

# Frontend URL (used for CORS/CSRF)
# - Local: http://localhost:3000
# - Docker Dev: http://localhost:3000
# - Docker Prod: https://chat.lite.space
FRONTEND_URL=http://localhost:3000

# Frontend Public Backend URL (exposed to browser)
# - Local: http://localhost:8000
# - Docker Dev: http://localhost:8000
# - Docker Prod: https://api.lite.space
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Frontend Public Payload URL (exposed to browser)
# - Local: http://localhost:3001
# - Docker Dev: http://localhost:3001
# - Docker Prod: https://cms.lite.space
NEXT_PUBLIC_PAYLOAD_URL=http://localhost:3001

# =============================================================================
# DATABASE CONNECTIONS (Different per environment)
# =============================================================================

# MongoDB Connection String
# - Local: mongodb://localhost:27017
# - Docker: mongodb://mongodb:27017
MONGO_URI=mongodb://localhost:27017

# MongoDB Details (alias for MONGO_URI, used by some services)
MONGO_DETAILS=mongodb://localhost:27017

# MongoDB URI (another alias)
MONGODB_URI=mongodb://localhost:27017

# Test MongoDB URI (for testing)
TEST_MONGO_URI=mongodb://localhost:27017

# Payload CMS Database URI
# - Local: mongodb://localhost:27017/payload_cms
# - Docker: mongodb://mongodb:27017/payload_cms
DATABASE_URI=mongodb://localhost:27017/payload_cms

# Redis Connection URL
# - Local: redis://localhost:6379/0
# - Docker: redis://redis:6379/0
REDIS_URL=redis://localhost:6379/0

# =============================================================================
# SECRETS (Store in service-specific .env files, not in root .env)
# =============================================================================
# These should be stored in:
# - backend/.env for GOOGLE_API_KEY
# - payload_cms/.env for PAYLOAD_SECRET
#
# GOOGLE_API_KEY=your-google-api-key-here
# PAYLOAD_SECRET=your-payload-secret-here

# =============================================================================
# CONFIGURATION (Usually same across environments)
# =============================================================================

# MongoDB Database Name
MONGO_DB_NAME=litecoin_rag_db

# MongoDB Collection Name for RAG documents
MONGO_COLLECTION_NAME=litecoin_docs

# MongoDB Database Name (alias)
MONGO_DATABASE_NAME=litecoin_rag_db

# CMS Articles Collection Name
CMS_ARTICLES_COLLECTION_NAME=cms_articles

# Embedding Model
EMBEDDING_MODEL=text-embedding-004

# CORS Origins (comma-separated)
# - Local/Docker Dev: http://localhost:3000,http://localhost:3001
# - Docker Prod: https://chat.lite.space,https://cms.lite.space
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Rate Limiting
RATE_LIMIT_PER_MINUTE=20
RATE_LIMIT_PER_HOUR=300

# Logging Configuration
LOG_LEVEL=INFO
JSON_LOGGING=false

# Node Environment
# - Development: development
# - Production: production
NODE_ENV=development

# =============================================================================
# MONITORING (Optional)
# =============================================================================

# Grafana Admin User
GRAFANA_ADMIN_USER=admin

# Grafana Admin Password
GRAFANA_ADMIN_PASSWORD=admin

# Cloudflare Tunnel Token (for production)
# CLOUDFLARE_TUNNEL_TOKEN=your-tunnel-token-here

# =============================================================================
# DOCKER-SPECIFIC (Only used in Docker environments)
# =============================================================================

# Watchpack Polling (for hot reload in Docker)
WATCHPACK_POLLING=true

# Hostname (for Next.js in Docker)
HOSTNAME=0.0.0.0
