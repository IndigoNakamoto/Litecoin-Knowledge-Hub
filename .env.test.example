# Test Environment Configuration
# ================================
# This file shares Payload CMS database with production while using
# separate MongoDB and Redis instances for backend testing.
#
# Copy this file to .env.test and configure the values below.
# cp .env.test.example .env.test
#
# IMPORTANT: Payload CMS connects to your PRODUCTION database.
# Any article edits will affect PRODUCTION data.

# =============================================================================
# Production MongoDB Connection (for Payload CMS)
# =============================================================================
# Point to your PRODUCTION MongoDB instance for the payload_cms database.
# This allows testing with real articles without duplicating content.
#
# Format examples:
#   Local: mongodb://username:password@localhost:27017/payload_cms?authSource=admin
#   Atlas: mongodb+srv://username:password@cluster.mongodb.net/payload_cms?retryWrites=true&w=majority
#
PROD_DATABASE_URI=mongodb://your-prod-user:your-prod-password@your-prod-host:27017/payload_cms?authSource=admin

# =============================================================================
# Test MongoDB Connection (for Backend Vector Store)
# =============================================================================
# Uses local test MongoDB container (mongodb_test:27017)
# This keeps the vector store separate from production.
# Override only if you want to use a different test instance.
#
TEST_MONGO_URI=mongodb://mongodb_test:27017/litecoin_rag_db

# =============================================================================
# Test Redis Connection
# =============================================================================
# Uses local test Redis container
# This keeps the cache separate from production.
#
TEST_REDIS_URL=redis://redis_test:6379/0

# =============================================================================
# Test Service URLs (for frontend build args)
# =============================================================================
# These URLs are baked into the frontend at build time.
#
TEST_BACKEND_URL=http://localhost:8001
TEST_PAYLOAD_URL=http://localhost:3002

# =============================================================================
# Optional: MongoDB Authentication for Test Instance
# =============================================================================
# If you want authentication on the test MongoDB container, set these:
#
# MONGO_ROOT_USERNAME=admin
# MONGO_ROOT_PASSWORD=your-test-mongo-password

# =============================================================================
# Optional: Redis Authentication for Test Instance
# =============================================================================
# If you want authentication on the test Redis container, set this:
#
# REDIS_PASSWORD=your-test-redis-password

# =============================================================================
# Grafana (if running monitoring in test)
# =============================================================================
# Required if you include prometheus/grafana in test environment
#
# GRAFANA_ADMIN_PASSWORD=test-grafana-password
